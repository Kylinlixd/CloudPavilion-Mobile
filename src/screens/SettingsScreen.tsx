import { useEffect, useState } from 'react'
import { Alert, ScrollView, Text, TextInput, View } from 'react-native'
import { useNavigation } from '@react-navigation/native'

import { ActionButton } from '../components/ActionButton'
import { AppHeader } from '../components/AppHeader'
import { EmptyState } from '../components/EmptyState'
import { useAuth } from '../context/AuthContext'
import { useFamily } from '../context/FamilyContext'
import { apiClient } from '../lib/api'
import type { Family, Membership } from '../lib/types'
import { colors } from '../theme/colors'
import { spacing } from '../theme/spacing'
import { typography } from '../theme/typography'

function asList<T>(value: T[] | { results: T[] }) { return Array.isArray(value) ? value : value.results }
export function SettingsScreen() { const navigation = useNavigation<any>(); const { logout } = useAuth(); const { familyId, family, setCurrentFamilyId, refreshFamily } = useFamily(); const [input, setInput] = useState(familyId || ''); const [members, setMembers] = useState<Membership[]>([]); const [name, setName] = useState(''); useEffect(() => { if (!familyId) return; void Promise.all([refreshFamily(), apiClient.get<Membership[] | { results: Membership[] }>('/memberships/')]).then(([, value]) => setMembers(asList(value))) }, [familyId, refreshFamily]); async function saveFamily() { await setCurrentFamilyId(input); await refreshFamily().catch(() => undefined); Alert.alert('家庭已切换', `当前家庭 ID：${input}`) } async function createFamily() { if (!name.trim()) return; const created = await apiClient.post<Family>('/families/', { name: name.trim() }); await setCurrentFamilyId(String(created.id)); setName(''); Alert.alert('家庭已创建', created.name) } return <ScrollView contentContainerStyle={{ padding: spacing.lg, paddingBottom: 120 }} style={{ backgroundColor: colors.paper }}><AppHeader title="设置" /><View style={{ backgroundColor: colors.ink, borderRadius: 16, padding: spacing.xl }}><Text style={{ color: colors.terracottaLight, fontFamily: typography.mono, fontSize: 10, letterSpacing: 1 }}>当前家庭</Text><Text style={{ color: colors.white, fontFamily: typography.display, fontSize: 28, marginTop: 22 }}>{family?.name || (familyId ? `家庭 ${familyId}` : '还没有选择')}</Text><Text style={{ color: 'rgba(255,255,255,.62)', fontFamily: typography.body, fontSize: 12, marginTop: 7 }}>家庭 ID {familyId || '—'}</Text><TextInput keyboardType="number-pad" onChangeText={setInput} placeholder="输入家庭 ID" placeholderTextColor="rgba(255,255,255,.45)" style={{ borderBottomColor: 'rgba(255,255,255,.26)', borderBottomWidth: 1, color: colors.white, marginTop: 24, paddingBottom: 10 }} /><View style={{ marginTop: spacing.lg }}><ActionButton onPress={() => void saveFamily()}>切换家庭</ActionButton></View></View><View style={{ backgroundColor: colors.paperBright, borderRadius: 16, marginTop: spacing.md, padding: spacing.xl }}><Text style={{ color: colors.terracotta, fontFamily: typography.mono, fontSize: 10, letterSpacing: 1 }}>创建新家庭</Text><Text style={{ color: colors.ink, fontFamily: typography.display, fontSize: 24, marginTop: 20 }}>从一个名字开始。</Text><TextInput onChangeText={setName} placeholder="例如：周末阅读会" placeholderTextColor={colors.muted} style={{ borderBottomColor: colors.line, borderBottomWidth: 1, color: colors.ink, marginTop: 20, paddingBottom: 10 }} /><View style={{ marginTop: spacing.lg }}><ActionButton onPress={() => void createFamily()}>创建家庭</ActionButton></View></View><Text style={{ color: colors.ink, fontFamily: typography.display, fontSize: 25, marginTop: spacing.xxl }}>家庭成员</Text><View style={{ marginTop: spacing.md }}>{members.length ? members.map((member) => <View key={member.id} style={{ alignItems: 'center', borderBottomColor: colors.line, borderBottomWidth: 1, flexDirection: 'row', justifyContent: 'space-between', paddingVertical: spacing.md }}><View><Text style={{ color: colors.ink, fontFamily: typography.body, fontSize: 15 }}>{member.user_detail.nickname || member.user_detail.username}</Text><Text style={{ color: colors.muted, fontFamily: typography.body, fontSize: 11, marginTop: 4 }}>{member.user_detail.email || '未留下邮箱'}</Text></View><Text style={{ color: colors.terracotta, fontFamily: typography.mono, fontSize: 10 }}>{member.role}</Text></View>) : <EmptyState title="暂时没有成员资料" copy="确认家庭 ID 正确后再试一次。" />}</View><View style={{ marginTop: spacing.xxl }}><ActionButton onPress={() => { void logout(); navigation.navigate('Login') }} quiet>退出登录</ActionButton></View></ScrollView> }
